#ifndef pathfinder_h_seen
#define pathfinder_h_seen

/* find the astar from "from" to "to", and return the next best index
 * can return -1 if it gets mixed up! */
int path_find(int from, int to);

#endif
